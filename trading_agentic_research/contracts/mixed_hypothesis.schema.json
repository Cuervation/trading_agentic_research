{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "MixedHypothesis",
  "type": "object",
  "required": ["hypothesis_id", "source_ids", "claim", "causal_mechanism", "mixed_mechanism", "implementation_change", "expected_effect", "falsification_rule", "materiality_guard"],
  "properties": {
    "source_ids": {"type": "array", "minItems": 2, "items": {"type": "string"}},
    "mixed_mechanism": {"type": "string", "minLength": 1}
  },
  "additionalProperties": true
}
